import React from 'react';
import { Container } from 'react-bootstrap';

const Verificacion = () => (
  <Container className="py-5">
    <h1>Verificación de identidad</h1>
    <p>Proceso de verificación (placeholder).</p>
  </Container>
);

export default Verificacion;
